# Supabase Configuration
# Get these values from: https://supabase.com/dashboard
# 1. Create a new project or select existing one
# 2. Go to Project Settings > API
# 3. Copy the Project URL and Anon Key
NEXT_PUBLIC_SUPABASE_URL=https://your_supabase_project_id.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Supabase Project ID (Required for type generation)
# Extract from your Supabase URL: https://[PROJECT_ID].supabase.co
# Example: If URL is https://abc123def.supabase.co, then PROJECT_ID is abc123def
SUPABASE_PROJECT_ID=your_supabase_project_id

# Supabase Development (Optional)
# Used for local development callbacks
NEXT_PUBLIC_DEV_SUPABASE_REDIRECT_URL=http://localhost:3000

# Supabase Service Keys (Optional - Server-side only)
# Get from Project Settings > API > Service Role Key
# WARNING: Never expose this key in client-side code
SUPABASE_SERVICE_ROLE_KEY=your_supabase_service_role_key

# Sentry Error Tracking (Optional)
# Get from: https://sentry.io
# 1. Create a new organization/project
# 2. Go to Settings > Client Keys (DSN)
# 3. Copy the DSN value (remove https:// prefix)
SENTRY_DSN=your_sentry_dsn

# Sentry Auth Token (Optional - For deployment)
# Get from Sentry > Settings > Auth Tokens
SENTRY_AUTH_TOKEN=your_sentry_auth_token

# Datadog Configuration
# Get from: https://app.datadoghq.com/

# Datadog API Key (Required for server-side log transport)
# Get from: Organization Settings > API Keys
# WARNING: Server-side secret, never expose in client code
DATADOG_API_KEY=your_datadog_api_key

# Datadog RUM Configuration (Optional - for client-side monitoring)
# Get from: RUM > Applications > Create Application
NEXT_PUBLIC_DATADOG_APPLICATION_ID=0903e4a4-5a20-4439-8ba7-43c4101dfbd9
NEXT_PUBLIC_DATADOG_CLIENT_TOKEN=pub1229965fea4a6ca5f1bab5f8f34e5177
NEXT_PUBLIC_DATADOG_SITE=us5.datadoghq.com
NEXT_PUBLIC_DATADOG_SERVICE=structura
NEXT_PUBLIC_DATADOG_ENV=development
NEXT_PUBLIC_APP_VERSION=1.0.0

# Logging Configuration
# Options: debug, info, warn, error
LOG_LEVEL=info

# Environment
NODE_ENV=development

# IP Geolocation API (Optional)
# Get from: https://ipgeolocation.io
# 1. Sign up for free account
# 2. Go to Dashboard > API Keys
# 3. Copy your API key
IPGEOLOCATION_API_KEY=your_ipgeolocation_api_key

# Security Configuration
# CSRF Protection Secret (Required in production)
# Generate a secure random string (32+ characters)
# Example: openssl rand -hex 32
CSRF_SECRET=your_csrf_secret_32_chars_minimum

# Rate Limiting Storage (Production)
# Choose ONE of the following options for production deployments:

# Option 1: Vercel KV (Recommended for Vercel deployments)
# Get from: https://vercel.com/dashboard/stores
# KV_REST_API_URL=your_vercel_kv_rest_api_url
# KV_REST_API_TOKEN=your_vercel_kv_rest_api_token

# Option 2: Redis (Recommended for other deployments)
# WARNING: Untested!
# REDIS_URL=redis://localhost:6379
# Or individual Redis settings:
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=your_redis_password
